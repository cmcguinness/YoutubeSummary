# Copy to .env and fill in. .env is git-ignored — never commit real values.

OPENAI_API_KEY=sk-...

# Generate with:  python userauth.py <username> <password>
USERDB={"admin": "scrypt:32768:8:1$...."}

# Any long random string; python -c "import secrets; print(secrets.token_hex(32))"
SESSION_KEY=

# Optional
# Set to true on a trusted LAN host to remove the login entirely
# DISABLE_AUTH=true
# OPENAI_MODEL=gpt-5.6-luna
# HTTPS_ONLY=true
